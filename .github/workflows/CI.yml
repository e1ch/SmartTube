name: Build & Release

on:
  push:
    branches: [ "master" ]
  workflow_dispatch:

permissions:
  contents: write

jobs:
  build:
    runs-on: ubuntu-latest
    env:
      HAS_SIGNING_KEY: ${{ secrets.SIGNING_KEY != '' }}

    steps:
      - name: Checkout Code
        uses: actions/checkout@v4
        with:
          submodules: recursive
          fetch-depth: 0

      - name: Set up JDK 17
        uses: actions/setup-java@v4
        with:
          java-version: '17'
          distribution: 'temurin'
          cache: 'gradle'

      - name: Read Version
        id: version
        shell: bash
        run: |
          VERSION_NAME="$(tr -d '\r\n' < VERSION)"
          if [ -z "$VERSION_NAME" ]; then
            echo "VERSION file is empty"
            exit 1
          fi

          VERSION_TAG="v${VERSION_NAME}"

          echo "version_name=$VERSION_NAME" >> "$GITHUB_OUTPUT"
          echo "version_tag=$VERSION_TAG" >> "$GITHUB_OUTPUT"

          echo "VERSION_NAME=$VERSION_NAME"
          echo "VERSION_TAG=$VERSION_TAG"

      - name: Configure Build Signing
        if: ${{ env.HAS_SIGNING_KEY == 'true' }}
        shell: bash
        run: |
          echo "storePassword=${{ secrets.KEY_STORE_PASSWORD }}" > keystore.properties
          echo "keyAlias=${{ secrets.ALIAS }}" >> keystore.properties
          echo "keyPassword=${{ secrets.KEY_PASSWORD }}" >> keystore.properties
          echo "storeFile=${{ github.workspace }}/key.jks" >> keystore.properties
          echo "${{ secrets.SIGNING_KEY }}" | base64 --decode > "${{ github.workspace }}/key.jks"

      - name: Grant Gradle permissions
        run: chmod +x gradlew

      - name: Build Beta Release APKs
        run: ./gradlew assembleStbetaRelease

      - name: Build Stable Release APKs
        run: ./gradlew assembleStstableRelease

      - name: Prepare Release Assets
        shell: bash
        run: |
          mkdir -p release_assets

          for abi in armeabi-v7a arm64-v8a x86 universal; do
            cp smarttubetv/build/outputs/apk/stbeta/release/*_"${abi}".apk "release_assets/SmartTube_beta_${abi}.apk"
            cp smarttubetv/build/outputs/apk/ststable/release/*_"${abi}".apk "release_assets/SmartTube_stable_${abi}.apk"
          done

          ls -lah release_assets

      - name: Generate Release Notes
        shell: bash
        run: |
          {
            echo "## ${{ steps.version.outputs.version_tag }}"
            echo
            echo "Source commit: \`${{ github.sha }}\`"
            echo
            echo "### Assets"
            echo "- SmartTube_beta_armeabi-v7a.apk"
            echo "- SmartTube_beta_arm64-v8a.apk"
            echo "- SmartTube_beta_x86.apk"
            echo "- SmartTube_beta_universal.apk"
            echo "- SmartTube_stable_armeabi-v7a.apk"
            echo "- SmartTube_stable_arm64-v8a.apk"
            echo "- SmartTube_stable_x86.apk"
            echo "- SmartTube_stable_universal.apk"
            echo
            echo "### Recent commits"
            git log --pretty='- %s' -n 12
          } > release_notes.txt

      - name: Delete old moving release tag
        shell: bash
        run: |
          git push origin :refs/tags/release || true
          gh release delete release --yes || true
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      - name: Delete existing version tag release if exists
        shell: bash
        run: |
          gh release delete "${{ steps.version.outputs.version_tag }}" --yes || true
          git push origin ":refs/tags/${{ steps.version.outputs.version_tag }}" || true
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      - name: Create immutable version tag release
        shell: bash
        run: |
          gh release create "${{ steps.version.outputs.version_tag }}" \
            release_assets/* \
            --title "${{ steps.version.outputs.version_tag }}" \
            --notes-file release_notes.txt
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      - name: Create moving release tag
        shell: bash
        run: |
          gh release create release \
            release_assets/* \
            --title "${{ steps.version.outputs.version_tag }}" \
            --notes-file release_notes.txt
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      - name: Upload workflow artifacts
        uses: actions/upload-artifact@v4
        with:
          name: SmartTube_${{ steps.version.outputs.version_tag }}
          path: release_assets/*.apk
          if-no-files-found: error
          retention-days: 7
